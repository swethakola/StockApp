Perform Maven build using 
	StockApp - > mvn clean install
Above step builds jar with UI and Service files. 
Run the application from jar:
	java -jar stockDetailsService/target/stockApp-0.0.1.jar.
	
OR

Run applciation from service 
	stockDetailsService -> mvn spring-boot:run


Added file to upload in StockApp directory to check the format and columns - Filename -dow_jones_index.data